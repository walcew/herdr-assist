# Dash 2.0 — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Card de custo estimado (Agora/Semana/Vitalício) na Dash + cards de uso com pill corp/pessoal + domínio + contagem de agentes por conta.

**Architecture:** A ponte calcula custo por tokens×tabela de preços lendo os transcripts (cache por mtime/size, no executor), agrega Agora/Semana/Vitalício e difunde um payload `cost`; os cards de limite ganham `org`/`corp`/contagem. O firmware guarda e renderiza um card de custo no topo e o pill/contagem nos cards. Reaproveita `accounts.org_and_corp` e o mapa pane→transcript das features anteriores.

**Tech Stack:** Python 3.9+ stdlib (ponte), C11 + LVGL 8.4 + cJSON (firmware), PlatformIO.

**Spec:** `docs/superpowers/specs/2026-08-20-dash-2-design.md`

## Global Constraints

- Ponte: só stdlib, Python 3.9. Testes rodam de dentro de `plugin/` (`cd plugin && python -m unittest ...`); `python` (Windows).
- Custo é **estimativa** — sempre prefixo `~`; tabela de preços mantida na ponte; modelo desconhecido usa default (5/25).
- Retrocompat: campos/payloads novos ausentes = comportamento antigo.
- Perf: varredura de custo cacheada por `(mtime, size)`; leitura no `run_in_executor` (nunca no event loop); só relê arquivos que cresceram.
- Firmware: buffers fixos — `org[24]`, custo `char[16]` por valor; `agents`/`agents_working` uint8.
- Determinismo: strings de custo mudam devagar; dedup de snapshot preservado.
- Build firmware da tela: `python -m platformio run` (env `LVGL-320-480`). herdr_ui.c/ui_theme.h têm WIP não relacionado → isolar hunks no commit.
- Comentários/commits PT-BR acentuado.

---

## File Structure

- `plugin/cost.py` (NOVO) — PRICES, message_cost, fmt_usd, file_cost.
- `plugin/herdr_bridge.py` (MODIFICAR) — aggregate_cost + cache + payload cost; org/corp/contagem no payload de limits.
- `plugin/tests/test_cost.py` (NOVO) + fixtures.
- `plugin/tests/test_bridge_limits.py` (MODIFICAR) — org/corp/contagem.
- `src/herdr_model.h` (MODIFICAR) — campos em herdr_limits_t + herdr_cost_t + set/get.
- `src/herdr_model.c` (MODIFICAR) — armazenamento thread-safe de cost.
- `src/herdr_conn.c` (MODIFICAR) — parse em handle_limits + handle_cost.
- `src/ui_theme.h` — UI_CORP já existe (Sessões 2.0); reusar.
- `src/herdr_ui.c` (MODIFICAR) — card de custo + pill/org/contagem nos cards de uso.

---

## Task 1: Ponte — custo puro (`cost.py`)

**Files:**
- Create: `plugin/cost.py`
- Create: `plugin/tests/test_cost.py`
- Create: `plugin/tests/fixtures/cost/sample.jsonl`

**Interfaces (produzidas):**
- `cost.PRICES: dict[str, tuple[float, float]]`
- `cost.message_cost(usage: dict, model_id: str) -> float`
- `cost.fmt_usd(v: float) -> str`
- `cost.file_cost(jsonl_path: str) -> dict` (`{"total": float, "days": {date: float}}`)

- [ ] **Step 1: Fixture + testes que falham**

`plugin/tests/fixtures/cost/sample.jsonl` (2 assistants em datas diferentes):
```
{"type":"assistant","timestamp":"2026-08-20T10:00:00Z","message":{"role":"assistant","model":"claude-opus-4-8","usage":{"input_tokens":1000,"output_tokens":2000,"cache_read_input_tokens":0,"cache_creation_input_tokens":0}}}
{"type":"assistant","timestamp":"2026-08-19T10:00:00Z","message":{"role":"assistant","model":"claude-haiku-4-5","usage":{"input_tokens":1000,"output_tokens":0,"cache_read_input_tokens":0,"cache_creation_input_tokens":0}}}
```

`plugin/tests/test_cost.py`:
```python
# cd plugin && python -m unittest tests.test_cost -v
import os, unittest
import cost

FIX = os.path.join(os.path.dirname(__file__), "fixtures", "cost", "sample.jsonl")


class TestMessageCost(unittest.TestCase):
    def test_opus(self):
        # 1000*5 + 2000*25 = 55000 → /1e6 = 0.055
        c = cost.message_cost({"input_tokens": 1000, "output_tokens": 2000}, "claude-opus-4-8")
        self.assertAlmostEqual(c, 0.055, places=6)

    def test_cache_read_e_write(self):
        # cache_read 0.1x, cache_creation 1.25x do input price
        c = cost.message_cost({"cache_read_input_tokens": 1_000_000,
                               "cache_creation_input_tokens": 1_000_000}, "claude-opus-4-8")
        self.assertAlmostEqual(c, 5*0.1 + 5*1.25, places=6)  # 0.5 + 6.25

    def test_modelo_desconhecido_usa_default(self):
        c = cost.message_cost({"input_tokens": 1_000_000}, "modelo-xyz")
        self.assertAlmostEqual(c, 5.0, places=6)  # default (5, 25)


class TestFmtUsd(unittest.TestCase):
    def test_decimais_virgula(self):
        self.assertEqual(cost.fmt_usd(4.2), "~US$ 4,20")

    def test_milhar_ponto(self):
        self.assertEqual(cost.fmt_usd(1240), "~US$ 1.240")


class TestFileCost(unittest.TestCase):
    def test_total_e_days(self):
        m = cost.file_cost(FIX)
        # opus: 1000*5+2000*25=55000/1e6=0.055 ; haiku: 1000*1=1000/1e6=0.001
        self.assertAlmostEqual(m["total"], 0.056, places=6)
        self.assertAlmostEqual(m["days"]["2026-08-20"], 0.055, places=6)
        self.assertAlmostEqual(m["days"]["2026-08-19"], 0.001, places=6)

    def test_ausente_none(self):
        self.assertIsNone(cost.file_cost("/naoexiste.jsonl"))


if __name__ == "__main__":
    unittest.main()
```

- [ ] **Step 2: Rodar e ver falhar**

Run: `cd plugin && python -m unittest tests.test_cost -v`
Expected: FAIL (`No module named 'cost'`).

- [ ] **Step 3: Implementar `cost.py`**

```python
# plugin/cost.py
#!/usr/bin/env python3
"""Custo estimado a partir dos transcripts do Claude Code (só stdlib).

O transcript não guarda custo; estima-se tokens × tabela de preços por modelo.
Preços da API Anthropic (US$ por 1M tokens); cache read 0.1×, write 1.25× do
input. É estimativa — a UI mostra tudo com prefixo "~".
"""
from __future__ import annotations

import json

# model_id -> (input US$/1M, output US$/1M)
PRICES = {
    "claude-opus-4-8": (5.0, 25.0), "claude-opus-4-7": (5.0, 25.0),
    "claude-opus-4-6": (5.0, 25.0), "claude-opus-4-5": (5.0, 25.0),
    "claude-sonnet-5": (3.0, 15.0), "claude-sonnet-4-6": (3.0, 15.0),
    "claude-sonnet-4-5": (3.0, 15.0), "claude-haiku-4-5": (1.0, 5.0),
    "claude-fable-5": (10.0, 50.0), "claude-mythos-5": (10.0, 50.0),
}
_DEFAULT = (5.0, 25.0)


def message_cost(usage: dict, model_id: str) -> float:
    """US$ de um evento assistant: input + cache(write 1.25×, read 0.1×) + output."""
    in_p, out_p = PRICES.get(model_id or "", _DEFAULT)
    u = usage or {}
    inp = u.get("input_tokens") or 0
    out = u.get("output_tokens") or 0
    cr = u.get("cache_read_input_tokens") or 0
    cw = u.get("cache_creation_input_tokens") or 0
    return (inp * in_p + cw * in_p * 1.25 + cr * in_p * 0.1 + out * out_p) / 1_000_000.0


def fmt_usd(v: float) -> str:
    """"~US$ 4,20" / "~US$ 1.240" — vírgula decimal, ponto de milhar, prefixo ~."""
    if v >= 100:                       # valores grandes: sem centavos
        s = "%.0f" % v
    else:
        s = "%.2f" % v
    intp, _, frac = s.partition(".")
    # milhar com ponto
    grouped = ""
    for i, ch in enumerate(reversed(intp)):
        if i and i % 3 == 0:
            grouped = "." + grouped
        grouped = ch + grouped
    return "~US$ " + (grouped + ("," + frac if frac else ""))


def file_cost(jsonl_path: str):
    """{"total": float, "days": {date: float}} do transcript, ou None em falha."""
    total = 0.0
    days = {}
    try:
        with open(jsonl_path, encoding="utf-8", errors="replace") as fh:
            for line in fh:
                try:
                    o = json.loads(line)
                except ValueError:
                    continue
                if not isinstance(o, dict):
                    continue
                msg = o.get("message")
                if not (isinstance(msg, dict) and msg.get("role") == "assistant"):
                    continue
                c = message_cost(msg.get("usage") or {}, msg.get("model", ""))
                total += c
                ts = o.get("timestamp") or ""
                day = ts[:10]            # "YYYY-MM-DD"
                if day:
                    days[day] = days.get(day, 0.0) + c
    except OSError:
        return None
    return {"total": total, "days": days}
```

- [ ] **Step 4: Rodar e ver passar**

Run: `cd plugin && python -m unittest tests.test_cost -v`
Expected: PASS.

- [ ] **Step 5: Suíte inteira**

Run: `cd plugin && python -m unittest discover -s tests -v`
Expected: PASS (sem regressão).

- [ ] **Step 6: Commit**

```bash
git add plugin/cost.py plugin/tests/test_cost.py plugin/tests/fixtures/cost/
git commit -m "feat(ponte): custo estimado por tokens×preço (cost.py)"
```

---

## Task 2: Ponte — agregação + payload de custo (`herdr_bridge.py`)

**Files:**
- Modify: `plugin/herdr_bridge.py`

**Interfaces:**
- Consumes: `cost.file_cost`, `cost.fmt_usd` (Task 1); o mapa pane→transcript e `account_dirs` das features anteriores.
- Produces: `aggregate_cost(account_dirs, session_paths) -> dict` (`{"now","week","life"}` strings); payload `{"type":"cost", ...}` difundido.

> **Verificação:** integração com I/O real — a lógica pura é da Task 1. Aqui: suíte não regride + import smoke; validação ao vivo é do controlador.

- [ ] **Step 1: Import e caches**

Topo: `import cost`. Perto dos caches: 
```python
cost_cache: dict = {}       # path -> (mtime, size, {"total","days"})
last_cost_snapshot = ""     # dedup do payload cost


def _file_cost_cached(path):
    try:
        st = os.stat(path)
    except OSError:
        return None
    key = (st.st_mtime, st.st_size)
    ent = cost_cache.get(path)
    if not ent or ent[0] != key[0] or ent[1] != key[1]:
        m = cost.file_cost(path)
        cost_cache[path] = (key[0], key[1], m or {"total": 0.0, "days": {}})
    return cost_cache[path][2]
```

- [ ] **Step 2: Agregação (no executor)**

```python
import datetime as _dt

def aggregate_cost(account_dirs, session_paths):
    """Custo agora/semana/vitalício. Roda no executor (varredura de arquivos)."""
    seen = set()
    life = 0.0
    week = 0.0
    # janela [hoje-6 .. hoje]
    today = _dt.date.today()
    window = {(today - _dt.timedelta(days=i)).isoformat() for i in range(7)}
    for agent, cdir in account_dirs:
        if agent != "claude":
            continue
        import glob
        for p in glob.glob(os.path.join(cdir, "projects", "*", "*.jsonl")):
            if p in seen:
                continue
            seen.add(p)
            m = _file_cost_cached(p)
            if not m:
                continue
            life += m["total"]
            for day, c in m["days"].items():
                if day in window:
                    week += c
    now = 0.0
    for p in session_paths:                 # transcripts dos panes claude ativos
        m = _file_cost_cached(p)
        if m:
            now += m["total"]
    return {"now": cost.fmt_usd(now), "week": cost.fmt_usd(week),
            "life": cost.fmt_usd(life)}
```
(`_dt.date.today()` é permitido na ponte — não é o ambiente restrito do
Workflow; usa o relógio do host.)

- [ ] **Step 3: Laço de coleta + broadcast**

Reusar o `limits_loop` (ou um laço próprio com passo ~30s). Ao coletar limites,
também agregar custo no executor e difundir com dedup:
```python
            loop = asyncio.get_running_loop()
            costs = await loop.run_in_executor(
                None, aggregate_cost, set(account_dirs), list(_session_paths()))
            snap = json.dumps({"type": "cost", **costs}, separators=(",", ":"))
            global last_cost_snapshot
            if snap != last_cost_snapshot:
                last_cost_snapshot = snap
                await broadcast_line(snap)
```
`_session_paths()` devolve os transcripts dos panes claude atuais (mesma
resolução por UUID/cwd das Sessões 2.0 — extrair num helper se ainda não houver).
Entregar `last_cost_snapshot` no `handle_client` como os outros snapshots.

- [ ] **Step 4: Smoke + suíte**

Run: `cd plugin && python -c "import herdr_bridge"`
Run: `cd plugin && python -m unittest discover -s tests -v`
Expected: ambos OK.

- [ ] **Step 5: Commit**

```bash
git add plugin/herdr_bridge.py
git commit -m "feat(ponte): agrega custo (agora/semana/vitalício) e difunde payload cost"
```

---

## Task 3: Ponte — org/corp e contagem de agentes nos limites

**Files:**
- Modify: `plugin/herdr_bridge.py` (`collect_limits`/payload de limits)
- Modify: `plugin/tests/test_bridge_limits.py`

**Interfaces:**
- Consumes: `accounts.org_and_corp` (Sessões 2.0), `pane_account`.
- Produces: cada provedor de `limits` ganha `org`, `corp`, `agents`, `agents_working`.

- [ ] **Step 1: Teste que falha**

Adicionar a `test_bridge_limits.py`:
```python
class TestLimitsOrgCorp(unittest.TestCase):
    def test_provider_tem_org_corp(self):
        with mock.patch.object(b, "fetch_json", fake_usage):
            cur = b.collect_claude(os.path.join(FIX, "work"))
        # collect_claude não faz org/corp; o enriquecimento é no payload.
        # Testar a função de enriquecimento pura:
        prov = {"name": "Claude", "account": "bruno@sosdocs.com.br"}
        b.enrich_provider(prov, agents=3, working=2)
        self.assertEqual(prov["org"], "sosdocs")
        self.assertTrue(prov["corp"])
        self.assertEqual(prov["agents"], 3)
        self.assertEqual(prov["agents_working"], 2)
```

- [ ] **Step 2: Ver falhar → implementar**

Run: `cd plugin && python -m unittest tests.test_bridge_limits -v` → FAIL (`enrich_provider`).

Adicionar helper puro e usá-lo ao montar o payload de limites:
```python
def enrich_provider(prov, agents, working):
    """Anexa org/corp (do account) e contagem de agentes ao provedor."""
    org, corp = accounts.org_and_corp(prov.get("account", ""))
    if org:
        prov["org"], prov["corp"] = org, corp
    prov["agents"], prov["agents_working"] = int(agents), int(working)
    return prov
```
No `limits_loop`, ao ter `providers` e o `pane_account`/status dos agentes,
para cada provedor contar os panes cuja conta bate (agent kind + config_dir) e
chamar `enrich_provider`. (kind: "Claude"→"claude", "Codex"→"codex".)

- [ ] **Step 3: Ver passar + suíte**

Run: `cd plugin && python -m unittest discover -s tests -v` → PASS.

- [ ] **Step 4: Commit**

```bash
git add plugin/herdr_bridge.py plugin/tests/test_bridge_limits.py
git commit -m "feat(ponte): org/corp e contagem de agentes por conta no payload de limits"
```

---

## Task 4: Firmware — modelo + parse

**Files:**
- Modify: `src/herdr_model.h` (`herdr_limits_t`, novo `herdr_cost_t`, set/get)
- Modify: `src/herdr_model.c` (armazenamento de cost)
- Modify: `src/herdr_conn.c` (`handle_limits` + novo `handle_cost`)

**Interfaces:**
- Produces: `herdr_limits_t.{org[24],corp,agents,agents_working}`, `herdr_cost_t`, `herdr_model_set_cost`/`herdr_model_get_cost`.

- [ ] **Step 1: Campos + struct + API no modelo**

Em `herdr_model.h`, em `herdr_limits_t` (após `host`):
```c
    char    org[24];         /* domínio sem sufixo; "" desconhecido */
    bool    corp;            /* true = corporativa */
    uint8_t agents;          /* agentes desta conta */
    uint8_t agents_working;  /* quantos working */
```
Novo tipo + API (perto das outras):
```c
typedef struct {
    char now[16];   /* "~US$ 4,20" */
    char week[16];
    char life[16];
    bool valid;     /* false até chegar o primeiro payload cost */
} herdr_cost_t;

void herdr_model_set_cost(const herdr_cost_t *cost);
bool herdr_model_get_cost(herdr_cost_t *out);   /* false se nunca recebido */
```

- [ ] **Step 2: Armazenamento em `herdr_model.c`**

Adicionar um `static herdr_cost_t s_cost;` sob o mesmo lock do modelo e
implementar set/get (copiando sob lock; `set` marca `valid=true`; `get` devolve
`valid`). Seguir o padrão de `set_limits`/`get_limits`.

- [ ] **Step 3: Parse em `herdr_conn.c`**

Em `handle_limits`, no laço de provedores, após `plan`/`ok`, ler:
```c
        if (cJSON_IsString((f = cJSON_GetObjectItem(item, "org")))) {
            strncpy(l->org, f->valuestring, sizeof(l->org) - 1);
        }
        l->corp = cJSON_IsTrue(cJSON_GetObjectItem(item, "corp"));
        if (cJSON_IsNumber((f = cJSON_GetObjectItem(item, "agents")))) {
            l->agents = (uint8_t)f->valuedouble;
        }
        if (cJSON_IsNumber((f = cJSON_GetObjectItem(item, "agents_working")))) {
            l->agents_working = (uint8_t)f->valuedouble;
        }
```
Novo handler + roteamento no dispatch (junto de "agents"/"limits"):
```c
static void handle_cost(const cJSON *root)
{
    herdr_cost_t c;
    memset(&c, 0, sizeof(c));
    const cJSON *f;
    if (cJSON_IsString((f = cJSON_GetObjectItem(root, "now"))))
        strncpy(c.now, f->valuestring, sizeof(c.now) - 1);
    if (cJSON_IsString((f = cJSON_GetObjectItem(root, "week"))))
        strncpy(c.week, f->valuestring, sizeof(c.week) - 1);
    if (cJSON_IsString((f = cJSON_GetObjectItem(root, "life"))))
        strncpy(c.life, f->valuestring, sizeof(c.life) - 1);
    herdr_model_set_cost(&c);
}
```
e no `strcmp(t, ...)`: `else if (strcmp(t, "cost") == 0) handle_cost(root);`

- [ ] **Step 4: Build**

Run: `python -m platformio run` (env `LVGL-320-480`)
Expected: compila sem erro.

- [ ] **Step 5: Commit**

```bash
git add src/herdr_model.h src/herdr_model.c src/herdr_conn.c
git commit -m "feat(fw): campos de conta e custo no modelo + parse de cost"
```

---

## Task 5: Firmware — Dash 2.0 (card de custo + pill/org + contagem)

**Files:**
- Modify: `src/herdr_ui.c` (`add_limits_card`, `rebuild_dash_cards`)

**Interfaces:**
- Consumes: `herdr_limits_t.{org,corp,agents,agents_working}`, `herdr_model_get_cost` (Task 4); `UI_CORP` (já existe de Sessões 2.0).

> **Verificação:** UI LVGL — build + inspeção visual. Isolar hunks (WIP em herdr_ui.c).

- [ ] **Step 1: Card de custo no topo**

Em `rebuild_dash_cards`, antes do laço dos cards de uso:
```c
    herdr_cost_t cost;
    if (herdr_model_get_cost(&cost)) {
        lv_obj_t *cc = ui_card(s_dash_list, 10);
        lv_obj_set_size(cc, LV_PCT(100), 92);
        struct { const char *lab; const char *val; } rows[3] = {
            {T(STR_COST_NOW),  cost.now},
            {T(STR_COST_WEEK), cost.week},
            {T(STR_COST_LIFE), cost.life},
        };
        lv_obj_t *title = lv_label_create(cc);
        lv_label_set_text(title, T(STR_COST_TITLE));   /* "Custo · estimativa" */
        lv_obj_set_style_text_font(title, &lv_font_ui_14, 0);
        lv_obj_set_style_text_color(title, UI_TEXT, 0);
        lv_obj_align(title, LV_ALIGN_TOP_LEFT, DASH_PAD_X, DASH_PAD_TOP);
        for (int i = 0; i < 3; i++) {
            lv_obj_t *l = lv_label_create(cc);
            lv_label_set_text(l, rows[i].lab);
            lv_obj_set_style_text_font(l, &lv_font_ui_12, 0);
            lv_obj_set_style_text_color(l, UI_MUTED, 0);
            lv_obj_align(l, LV_ALIGN_TOP_LEFT, DASH_PAD_X, 30 + i * 18);
            lv_obj_t *v = lv_label_create(cc);
            lv_label_set_text(v, rows[i].val[0] ? rows[i].val : "—");
            lv_obj_set_style_text_font(v, &lv_font_ui_12, 0);
            lv_obj_set_style_text_color(v, UI_TEXT, 0);
            lv_obj_align(v, LV_ALIGN_TOP_RIGHT, -DASH_PAD_X, 30 + i * 18);
        }
    }
```
Strings novas em `i18n.h`/`i18n.c` (en/pt): `STR_COST_TITLE` ("Cost · estimate"/
"Custo · estimativa"), `STR_COST_NOW` ("Now"/"Agora"), `STR_COST_WEEK`
("Week"/"Semana"), `STR_COST_LIFE` ("Lifetime"/"Vitalício").

- [ ] **Step 2: Pill/org no título do card de uso**

Em `add_limits_card`, quando `show_acct` (multi-conta) e `l->org[0]`, trocar o
título `provedor · e-mail` por `provedor · [pill] org`: pill = `lv_label` curto
com `bg_opa` suave + radius (cor `UI_CORP` se `l->corp`, senão `UI_MUTED`), texto
"corp"/"pess"; depois `org`. (Reaproveitar o padrão do pill das Sessões 2.0.)

- [ ] **Step 3: Contagem de agentes no card**

Quando `l->agents > 0`, um `lv_label` no canto do card:
`agents_working` em destaque — texto `"%u (%u ativos)"` (agents, agents_working),
`lv_font_ui_12`, `UI_MUTED`, alinhado à direita abaixo do plano.

- [ ] **Step 4: Build**

Run: `python -m platformio run` (env `LVGL-320-480`)
Expected: compila sem erro.

- [ ] **Step 5: Verificação visual (manual)**

Card de custo no topo (Agora/Semana/Vitalício com `~US$`); cards de uso com pill
corp/pess + org (sem e-mail) e `N (M ativos)`.

- [ ] **Step 6: Commit (hunks isolados)**

```bash
git add -p src/herdr_ui.c   # só os hunks desta task
git add src/i18n.h src/i18n.c
git commit -m "feat(fw): Dash 2.0 — card de custo, pill/domínio e contagem por conta"
```

---

## Self-Review (autor do plano)

**Cobertura do spec:** custo puro (T1); agregação+payload (T2); org/corp+contagem (T3); modelo+parse+handle_cost (T4); UI card de custo + pill/org + contagem (T5). Perf (cache mtime/size + executor) T2. Degradação (get_cost false; org vazio; agents 0) T4/T5. ✔

**Placeholders:** nenhum; código real em cada passo. Ajustes visuais (posições do card de custo, largura do pill) marcados como inspeção no hardware.

**Consistência de tipos:** `org[24]`/`corp`/`agents`(uint8)/`agents_working`(uint8) no modelo (T4), lidos no parse (T4), usados na UI (T5); `herdr_cost_t.{now,week,life}[16]` set/get (T4) → UI (T5); custo `~US$` string em fmt_usd (T1) → aggregate_cost (T2) → payload → handle_cost (T4) → card (T5). `enrich_provider(prov, agents, working)` T3.
